import 'package:flutter/material.dart';

/// Professional anime-style color palette
class AppColors {
  // Primary colors - Vibrant but professional
  static const primary = Color(0xFF6366F1); // Indigo
  static const primaryDark = Color(0xFF4F46E5);
  static const primaryLight = Color(0xFF818CF8);

  // Secondary colors
  static const secondary = Color(0xFFEC4899); // Pink
  static const accent = Color(0xFF8B5CF6); // Purple

  // Background colors
  static const background = Color(0xFF0F172A); // Dark blue-gray
  static const surface = Color(0xFF1E293B);
  static const card = Color(0xFF334155);

  // Text colors
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFCBD5E1);
  static const textTertiary = Color(0xFF94A3B8);

  // Status colors
  static const success = Color(0xFF10B981); // Green
  static const warning = Color(0xFFF59E0B); // Orange
  static const error = Color(0xFFEF4444); // Red
  static const online = Color(0xFF22C55E); // Bright green
  static const offline = Color(0xFF6B7280); // Gray

  // Game lobby colors
  static const lobbyBackground = Color(0xFF1A1A2E);
  static const lobbyGround = Color(0xFF16213E);
  static const chatBubble = Color(0xFF0F3460);
  static const chatBubbleOwn = Color(0xFF533483);

  // Transparent overlays
  static const overlay = Color(0x80000000);
  static const overlayLight = Color(0x40000000);
}
